{% extends 'base.html.twig' %}

{% block title %}CaseCategory{% endblock %}

{% block body %}
    <h1>CaseCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ case_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ case_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ case_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_case_category_index') }}">back to list</a>

    <a href="{{ path('app_case_category_edit', {'id': case_category.id}) }}">edit</a>

    {{ include('case_category/_delete_form.html.twig') }}
{% endblock %}
